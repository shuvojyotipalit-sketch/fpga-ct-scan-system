# fpga-ct-scan-system
This project implements a simplified FPGA-based CT scan reconstruction system using Verilog. It processes input projection data through a Ram-Lak filter, performs back projection, and stores results in a frame buffer to generate image pixels. It demonstrates real-time, hardware-efficient CT imaging using a streaming architecture.
